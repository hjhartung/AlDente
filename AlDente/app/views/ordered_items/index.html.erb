<div class="page-header">
  <h1>Listing ordered items</h1>
</div>

<table class="table table-striped table-bordered table-condensed">
  <thead>
    <tr>
      <th>Item</th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @ordered_items.each do |ordered_item| %>
      <tr>
        <td><%= link_to ordered_item.item_id, edit_ordered_item_path(ordered_item) %></td>
        <td><%= link_to 'Show', ordered_item %></td>
        <td><%= link_to 'Destroy', ordered_item, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Ordered item', new_ordered_item_path %>
