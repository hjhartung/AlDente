<%= form_for(@ordered_item) do |f| %>
  <% if @ordered_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ordered_item.errors.count, "error") %> prohibited this ordered_item from being saved:</h2>

      <ul>
      <% @ordered_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :item_id %><br>
    <%= f.number_field :item_id %>
  </div>
    <div class="field">
    <%= f.label :order_id %><br>
    <%= f.number_field :order_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
